# Ultralytics ByteTrack profile for a fixed, low-frame-rate safety camera.
# Strong threshold starts an ID; low-score detections can only recover one.
tracker_type: bytetrack
track_high_thresh: 0.40
track_low_thresh: 0.10
new_track_thresh: 0.50
track_buffer: 45
match_thresh: 0.85
fuse_score: true
