# Generated worlds

`openarm_sorting.usd` is exported by `scripts/create_scene.py` and ignored by Git because it
contains a large referenced OpenArm cache. Python plus YAML are the reproducible source.

